import 'package:flutter/material.dart';

class MoviesNoSearchResultWidget extends StatelessWidget {
  const MoviesNoSearchResultWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("No results..."));
  }
}
